# Leave empty in development: Vite proxies /api to the FastAPI server.
# Set to the deployed API origin (no trailing slash) for production builds:
VITE_API_BASE_URL=https://asid-backend.onrender.com

# Where the dev proxy forwards /api requests in local development:
VITE_PROXY_TARGET=http://127.0.0.1:8000
