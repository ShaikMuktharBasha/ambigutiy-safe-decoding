# Leave empty in development: Vite proxies /api to the FastAPI server.
# Set to the deployed API origin (no trailing slash) for production builds, e.g. https://api.example.com
VITE_API_BASE_URL=

# Where the dev proxy forwards /api requests.
VITE_PROXY_TARGET=http://127.0.0.1:8000
